server:
  port: 8080

spring:
  r2dbc:
    url: r2dbc:h2:mem:///marketdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  sql:
    init:
      mode: never
  data:
    redis:
      host: localhost
      port: 6379

payment:
  enabled: true
  base-url: http://localhost:8081
